main =>
    number val => 7;
    for a(1 to val) =>
        write (CONSOLE) => a;
    >>

    for b(val to 0)[-2] =>
        write (CONSOLE) => "Second for";
    >>

    for c(0 to 10)[+1] =>
        write (CONSOLE) => "third for";
    >>
>>