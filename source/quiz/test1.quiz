main =>
    number n => 8 + 8;
    write (CONSOLE) => n;
    number as => 3 * 22;
    write (CONSOLE) => as;
    number sum => as + n;
    write (CONSOLE) => sum;
    write (CONSOLE) => "knewfkolnwel";
    write (CONSOLE) => 23;
    write (CONSOLE) => 20/2;
    write(CONSOLE) => q1.title;
>>