main =>
    map question mq => get("../questions/linguagem_questions.qst");
    map text mt;
    map number mn;
    map boolean mb;
    
    question q1;

    text t1 => "a b c d e";
    list text lt;
    lt => split(t1, " ");

    mq => get("cidades", q1);

    mn => put("eu", 2);
    mn => put("eu", "e");
    mn => put("eu", e);
    mn => put("eu", FALSE);
    mn => put(q1.title, 2);
    mn => put(q1.title, "e");
    mn => put(q1.title, e);
    mn => put(q1.title, FALSE);
    
    mt => clear();
>>